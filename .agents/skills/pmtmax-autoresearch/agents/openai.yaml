interface:
  display_name: "PM Tmax Autoresearch"
  short_description: "Run LGBM candidate YAML loops around the manifest baseline and gate winners safely."
  default_prompt: "Use $pmtmax-autoresearch to create or edit one candidate YAML, run autoresearch-step/gate/paper analysis, and promote winners without touching canonical aliases unless explicitly requested."

policy:
  allow_implicit_invocation: true
