run_tag: 20260410-lgbm-275k-expansion
candidate_name: p13_sub_q42reg
model_name: lgbm_emos
base_variant: recency_neighbor_oof
description: "Phase 13: subsample (p12 best) + q42 center + moderate reg.\np12_halflife7_subsample\
  \ achieved 0.7740 (new best) via stochastic trees. Now combining two independently\
  \ validated axes: subsample variance reduction + more aggressive center shift (q42\
  \ vs q45).\nq43 alone (p11) hurt CRPS (0.7773). But p12 showed subsample REDUCES\
  \ variance by 0.0022 \u2014 this variance budget can absorb a small center shift\
  \ without CRPS degradation. q42 = 3% more aggressive than q43, paired with reg_lambda=8\
  \ (slightly stronger than default 5) to prevent overfitting the shifted center.\n\
  If additive: 0.7740 - 0.001 \u2248 0.7730 target.\n"
params:
  n_estimators: 800
  num_leaves: 127
  max_depth: 8
  learning_rate: 0.03
  min_child_samples: 50
  use_recency_weights: true
  recency_half_life_days: 7.0
  use_oof_scale: false
  subsample_freq: 1
  subsample: 0.8
  colsample_bytree: 0.75
  reg_alpha: 0.5
  reg_lambda: 8.0
  use_quantile_loss: true
  use_neighbor_delta: true
  drop_dead_features: true
  quantile_center_alpha: 0.42
