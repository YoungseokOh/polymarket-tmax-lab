# Historical Real Collection Log

Append-only operational log for curated `historical_real` market, truth, and forecast collection.

| Run Date | Scope | Mode | Outcome | Markets | Notes |
| --- | --- | --- | --- | ---: | --- |
| 2026-04-25 | `historical_temperature_snapshots.json` | validate/preflight/trust + market/truth/forecast top-off | success | 2,021 | Inventory validation issues `0`; preflight/trust `ready=true`/`ok=true`; `backfill-markets` wrote `bronze_market_snapshots=5915`, `silver_market_specs=2111`; `backfill-truth` wrote `bronze_truth_snapshots=2516`, `silver_observations_daily=2342`; GFS `single_run` top-off wrote `single_run_ok=3618`, `single_run_err=0`; readiness now `forecast_ready=2021`, `truth_ready=2021`, `gold_missing=2021`. |
| 2026-04-25 | `historical_temperature_snapshots.json` | ECMWF single-run top-off + curated multi-source variant | success | 2,021 | `backfill-forecasts --model ecmwf_ifs025 --strict-archive --missing-only --max-consecutive-429 2` wrote `single_run_ok=3618`, `single_run_err=0`; no two-consecutive-`429` cancellation. Materialized `historical_training_set_curated_multisource_20260425` with `6063` rows / `2021` markets / `68` columns and panel `historical_backtest_panel_curated_multisource_20260425` with `49509` rows. Quick eval: baseline holdout CRPS_C `0.6749`, curated holdout CRPS_C `2.8771`; not promotable before variance/tail calibration. |
| 2026-04-26 | `historical_training_set_curated_multisource_20260425` | tail calibration experiment | diagnostic success | 2,021 | Added unit-aware `TailCalibratedGaussianModel` and ran `scripts/run_tail_calibration_experiment.py`. Root cause signal: `32F`/`0C` zero-Celsius sentinels appear in NWP daily-max features while availability remains true. Balanced tailcal reduced curated holdout CRPS_C `2.8771 -> 1.7195` and kept baseline holdout near canonical baseline (`0.8043`), but this is diagnostic only; fix sentinel feature validity upstream before promotion. |
| 2026-04-26 | `historical_training_set_curated_multisource_sentinelfix_20260426` | sentinel feature-validity fix + retrain | diagnostic success | 2,018 | `materialize_training_set` now rejects all-zero target-day temperature aggregates and falls back from invalid `single_run` to valid `historical_forecast` where possible. New dataset has `6054` rows; all-source sentinel rows `1083 -> 0`; dropped market ids `282535`, `285758`, `289186`. Retrained `lgbm_emos/high_neighbor_oof`; curated holdout CRPS_C `2.7690` remains worse than same-holdout GFS-only `2.1103`, so not promotable. |
| 2026-04-26 | `historical_training_set_curated_multisource_sentinelfix_20260426` | train-side source-gating experiment | diagnostic success | 2,018 | Added `SourceGatedGaussianModel` and ran `scripts/run_source_gating_experiment.py`. Binary/regret-weighted gates barely moved curated holdout CRPS_C (`2.7690 -> best 2.7607`) and stayed far worse than same-holdout GFS-only (`2.1103`). Baseline holdout was safe/slightly better (`0.4965 -> best 0.4932`). Gate selected fallback for only ~2% of curated holdout rows; tail losses are mostly city/region distribution shift, so this is not promotable. |
| 2026-04-26 | `historical_training_set_curated_multisource_sentinelfix_20260426` | disagreement calibration experiment | diagnostic success | 2,018 | Added `DisagreementCalibratedGaussianModel` and ran `scripts/run_disagreement_calibration_experiment.py`. Variance-only disagreement calibration improved curated holdout CRPS_C `2.7690 -> 2.2342` but did not beat GFS-only `2.1103`. Positive-disagreement mean shrink + variance calibration improved curated holdout to `1.3153`, Brier `0.0748`, ECE `0.0214`, but baseline holdout worsened from `0.4965` to `0.5627`; keep diagnostic only until validated with a non-holdout-tuned candidate and recent-core/backtest gates. |
| 2026-04-26 | `historical_temperature_snapshots.json` | targeted Ankara expansion + forecast/truth/price backfill | success | 2,041 | Refresh scanned `619` supported closed events, persisted `2,494` candidates, fetched `80`, and appended `20` Ankara URLs; no two-consecutive-`429` cancellation. Built/validated `20` target snapshots with issues `0`, merged local curated inventory to `2,041`, and trust-check stayed `ok=true`. Target forecast backfill wrote `single_run_ok=120`, `single_run_err=0`; target training set has `60` rows / `20` markets. Full local variant has `6114` rows / `2038` markets and panel `50070` token rows. Target price-history wrote `220` ok requests / `11036` points; target panel coverage is `ok=641`, `missing=14`, `stale=5`. |
| 2026-04-26 | `historical_temperature_snapshots.json` | targeted Dallas/Atlanta/Miami gap-fill + forecast/truth/price backfill | success | 2,101 | Gap-fill appended `60` URLs: Atlanta `20`, Dallas `20`, Miami `20`; no two-consecutive-`429` cancellation. Built/validated `60` target snapshots with issues `0`, merged local curated inventory to `2,101`, and trust-check stayed `ok=true`. Target forecast backfill wrote `single_run_ok=359`, `single_run_err=0`; target training set has `180` rows / `60` markets. Full local variant has `6294` rows / `2098` markets and panel `52050` token rows. Target price-history wrote `660` ok requests / `35365` points; target panel coverage is `ok=1941`, `missing=11`, `stale=28`. |
| 2026-04-27 | `historical_temperature_snapshots.json` | targeted Beijing/Chengdu/Chongqing/Madrid gap-fill + forecast/truth/price backfill | success | 2,169 | Gap-fill appended `68` URLs: Beijing `20`, Chengdu `21`, Chongqing `20`, Madrid `7`; no two-consecutive-`429` cancellation. Built/validated `68` target snapshots with issues `0`, merged local curated inventory to `2,169`, and trust-check stayed `ok=true`. Target forecast backfill wrote `single_run_ok=402`, `single_run_err=0`; target training set has `201` rows / `67` markets. Full local variant has `6495` rows / `2165` markets and panel `54261` token rows. Target price-history wrote `748` ok requests / `36030` points; target panel coverage is `ok=1868`, `missing=302`, `stale=41`. |
