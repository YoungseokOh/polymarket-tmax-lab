# Claude Project Notes

Primary repository guidance lives in `AGENTS.md`.

- Start with `AGENTS.md`, then `docs/codebase/index.md`.
- Use project skills in `.claude/skills/` and slash commands in `.claude/commands/`.
- Shared skill docs live in `docs/agent-skills/`.
- If `CLAUDE.md` and `AGENTS.md` ever differ, follow `AGENTS.md`.
